## Payment / PayOS implementation notes (FE)

### What was done
- **Booking payment screen** (`pages/student/booking/index.tsx`)
  - Uses PayOS QR/checkoutUrl returned from BE instead of hardcoded VietQR placeholder.
  - Button **Check Payment Status** now:
    - shows **"Chưa thanh toán"** when pending
    - only approves/reset UI when BE returns `paid=true`.
- **Payment menu page** (`pages/student/payment/index.tsx`)
  - Replaced mock invoices with real data from `getMyBookings()`.
  - Shows pending invoices (awaiting_payment) with:
    - **Pay now** (opens PayOS checkout)
    - **Check payment status**
    - **Cancel**
  - Shows basic history for approved/cancelled/expired bookings.
- **Types updated** (`src/lib/actions/booking.ts`)
  - Added `payos` info to booking item and submit/check responses.

### Key files changed
- `FEDMS/src/pages/student/booking/index.tsx`
- `FEDMS/src/pages/student/payment/index.tsx`
- `FEDMS/src/lib/actions/booking.ts`

### Runtime config
- FE keeps using `VITE_BASE_URL` to call BE.

